{
  "name": "medla-empresarial",
  "version": "1.0.0",
  "private": true,
  "packageManager": "pnpm@11.19.0",
  "description": "Sitio corporativo de MEDLA Empresarial",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/build.mjs && node --check app.js && node --check contacto.js && node --check servicios.js && node --check nosotros.js && node --check blog.js && node --check specialty.js && node --check api/contact.js && node scripts/check-site.mjs && node api/contact.test.js",
    "test": "node api/contact.test.js",
    "dev": "node scripts/dev-server.mjs"
  },
  "devDependencies": {
    "esbuild": "0.25.9"
  }
}
